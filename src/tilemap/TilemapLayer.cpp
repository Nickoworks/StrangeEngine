#include "TilemapLayer.h"

TilemapLayer::TilemapLayer(Uint16 id, Texture* tilesheet, int width, int height, int tilesize, int x, int y, double scale, bool visible) :
id(id), tilesheet(tilesheet), width(width), height(height), tilesize(tilesize), x(x), y(y), scale(scale), visible(visible){
    
}



